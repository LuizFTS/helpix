/**
 * Utilitários do módulo de Planejamento Financeiro.
 *
 * Etapa 01 (fundação): pasta criada só pra seguir o padrão das demais
 * features do projeto (ver `src/features/transactions/utils`,
 * `src/features/auth/utils`). Nenhuma função ainda — as próximas
 * etapas devem trazer aqui, por exemplo, helpers de data/projeção
 * usados pelo Projection Engine.
 */

export {};
